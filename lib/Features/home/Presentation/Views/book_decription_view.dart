import 'package:flutter/material.dart';

class BookDecriptionView extends StatelessWidget {
  const BookDecriptionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}